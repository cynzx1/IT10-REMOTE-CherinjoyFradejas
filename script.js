document.getElementById("nameForm").addEventListener("submit",function (event){
    event.preventDefault();

    const nameInput = document.getElementById("name").getElementById("name").value.trim(0);
    const correctname = "che";
});